# Railway ?????Code Migration?

**??: 1 ??????** — FastAPI + Next.js ? `Dockerfile.unified` ?????????

## 1. Dashboard ??????

1. [Railway](https://railway.com) ? **New Project** ? **Deploy from GitHub**
2. ????? `Code_Migration` ???
3. **Settings**

| ?? | ? |
|------|-----|
| **Root Directory** | ?????? |
| **Config file path** | `/railway.toml` |

4. **+ New** ? **Database** ? **PostgreSQL**
5. ??????? ? **Variables** ? **+ New Variable** ? **Reference** ? Postgres ? `DATABASE_URL`
6. **Variables** ? OpenAI ?????:

| Variable | ?? | ?? |
|----------|------|------|
| `OPENAI_API_KEY` | ???AI ???? | [OpenAI Platform](https://platform.openai.com/api-keys) ? API ?? |
| `OPENAI_MODEL` | ??? | ?? `gpt-4o-mini` |
| `DATABASE_URL` | ????????? | Postgres ??????? Reference |

7. **Deploy** / Redeploy

?? URL?? `https://xxx.up.railway.app`?? Web UI ?????????  
`/api/v1/convert` ? Next.js ??? FastAPI (`127.0.0.1:8091`) ?????????

### ????

| URL | ?? |
|-----|------|
| `/health` | Next.js ???? |
| `/api/v1/health` | Postgres / AI ???? |
| `/api/v1/setup` | ??????????`openai_api_key: set` ??? |

`OPENAI_API_KEY` ? `empty` ???? **?????** ??????Railway Variables ????? **Redeploy** ???????

## 2. CLI?`railway link`?

```powershell
railway login
railway link -p <Project-ID>
railway variables set OPENAI_API_KEY=sk-...
railway up
```

## 3. ??????

`.env.railway.example` ????????? `.env` ? Railway ????????? — **Railway Variables ???**?????

| Variable | ?? |
|----------|------|
| `OPENAI_API_KEY` | AI ????Railway ?????????????? |
| `OPENAI_ORG_ID` / `OPENAI_PROJECT` | OpenAI Platform ????????????? |
| `DATABASE_URL` | Postgres?Reference ??? |
| `DATABASE_PRIVATE_URL` | ????????????? `DATABASE_URL` ??? |

## 4. ????????

| ?? | ?? |
|------|------|
| ??????? | Variables ? `OPENAI_API_KEY` ?????? ? Redeploy |
| Postgres ??? | Postgres ????? + `DATABASE_URL` Reference |
| `/api/v1/setup` ? `openai_api_key: empty` | ???? `OPENAI_API_KEY`?????????? |

API ????????????? root ? `Dockerfile` ????`PORT` ???????????????????
